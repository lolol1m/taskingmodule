{{- if and .Values.gateway.enabled .Values.keycloak.gateway.enabled -}}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: keycloak
spec:
  parentRefs:
    - name: {{ .Values.gateway.name }}
  hostnames:
{{- range .Values.keycloak.gateway.hostnames }}
    - {{ . | quote }}
{{- end }}
  rules:
    - matches:
        - path:
            type: {{ .Values.keycloak.gateway.pathType }}
            value: {{ .Values.keycloak.gateway.path }}
      backendRefs:
        - name: keycloak
          port: {{ .Values.keycloak.service.port }}
{{- end }}