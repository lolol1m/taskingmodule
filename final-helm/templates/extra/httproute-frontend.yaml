{{- if and .Values.gateway.enabled .Values.frontend.gateway.enabled -}}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: frontend
spec:
  parentRefs:
    - name: {{ .Values.gateway.name }}
  hostnames:
{{- range .Values.frontend.gateway.hostnames }}
    - {{ . | quote }}
{{- end }}
  rules:
    - matches:
        - path:
            type: {{ .Values.frontend.gateway.pathType }}
            value: {{ .Values.frontend.gateway.path }}
      backendRefs:
        - name: frontend
          port: {{ .Values.frontend.service.port }}
{{- end }}